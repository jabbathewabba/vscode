{
  "name": "events-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "yarn workspace events-mobile dev",
    "dev:api": "yarn workspace events-api dev",
    "lint": "eslint . --ext .ts,.tsx,.js --report-unused-disable-directives --max-warnings=0",
    "format": "prettier --write ."
  }
}
